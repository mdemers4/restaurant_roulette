
<%= @restaurant.name %> <br>
<%= @restaurant.address %> <br>
<%= @restaurant.description %><br>

<% @reservations.each do |r| %>
<div>
  <%= User.find(r.user_id).name %> <br>
  <%= "Date: #{r.date}" %><br>
  <%= "Time: #{Restaurant.convert_time(r.time)}" %><br>
  <%= "Party size: #{r.size}" %><br>
  <br>
</div>
 <% end %>



<%= link_to "Back to Restaurants", user_restaurants_path %>

